$(function () {

	//'use strict';

	///**
	// * Init Admin Theme Core
	// * */
	//Core.init();

	///**
	// * Progress bar config
	// * */
	//NProgress.configure({
	//	minimum: 0.15,
	//	trickleRate: .07,
	//	trickleSpeed: 360,
	//	showSpinner: false,
	//	barColor: 'npr-danger', // npr-warning, npr-success, npr-primary, etc (all contextuals available)
	//	barPos: '' // 'null' - (default) - bar position: top of page
	//	// 'npr-bottom' -  bar position: bottom of page header
	//	// 'npr-header' -  bar position: below header
	//});

	///**
	// * Document Events
	// * */
	//$(document)
	//	.on('ajaxStart', function () {
	//		NProgress.start();
	//	})
	//	.on('ajaxStop', function () {
	//		NProgress.done();
	//	});
     //   //.on('ajaxError', function(XMLHttpRequest, response, errorThrown){
     //   //    if(typeof(response.responseText) !== 'undefined'){
     //   //        new PNotify({
     //   //            title: Lang.get('system', 'Notify'),
     //   //            text: response.responseText,
     //   //            type: 'danger',
     //   //            hide: false,
     //   //            width: "400px",
     //   //            buttons: {
     //   //                sticker: false,
     //   //                labels: {close: ""}
     //   //            }
     //   //        });
     //   //    }
     //   //})
    //
	});